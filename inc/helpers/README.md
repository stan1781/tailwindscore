# inc/helpers

Pure PHP helpers (no `echo`). Keep files small and named by concern:

- `manifest.php` — Vite manifest + dev origin helpers (foundation).

Add later: formatting utilities, query wrappers, WC-specific parsers — **never** template markup.
